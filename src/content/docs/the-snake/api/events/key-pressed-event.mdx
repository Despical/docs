---
title: KeyPressedEvent
description: Event fired from Paper player input before The Snake maps movement to a board direction.
---

`KeyPressedEvent` is produced from Paper's `PlayerInputEvent`. It represents `W`, `A`, `S`, `D`, or `NO_INPUT` for every observed player input update.

## Technical Overview

* **Package:** `dev.despical.snake.api.event.game`
* **Parent Class:** `SnakeEvent`
* **Cancellable:** No

| Method | Return Type | Description |
|:--|:--|:--|
| `getCode()` | `KeyCode` | One of `W`, `A`, `S`, `D`, or `NO_INPUT`. |
| `getPlayer()` | `Player` | Bukkit player whose input changed. |

The event can fire for players outside Snake arenas. The built-in arena listener filters by the assigned user and accepts at most one direction input per movement update.

```java
@EventHandler
public void onSnakeInput(KeyPressedEvent event) {
    if (event.getCode() != KeyPressedEvent.KeyCode.NO_INPUT) {
        getLogger().fine(event.getPlayer().getName() + " pressed " + event.getCode());
    }
}
```
